import { PlaceholderView } from '../../components/PlaceholderView'

export function PhotoView() {
  return (
    <PlaceholderView
      title="Prepare a photo"
      description="Upload or capture a photo, crop it to the required aspect ratio, resize it and compress it to the target file size — all on this device."
    />
  )
}

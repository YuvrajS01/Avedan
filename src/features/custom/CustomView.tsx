import { PlaceholderView } from '../../components/PlaceholderView'

export function CustomView() {
  return (
    <PlaceholderView
      title="Custom requirements"
      description="Enter your own pixel dimensions, aspect ratio, format and file-size limits to prepare any image."
    />
  )
}

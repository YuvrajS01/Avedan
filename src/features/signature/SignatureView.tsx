import { PlaceholderView } from '../../components/PlaceholderView'

export function SignatureView() {
  return (
    <PlaceholderView
      title="Prepare a signature"
      description="Upload or draw a signature, trim empty margins, resize it and compress it to the required file size — all on this device."
    />
  )
}
